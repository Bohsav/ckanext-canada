# README

## Overview

Dockerfile to build a CKAN instance pre-configured to be either a registry or a
portal.

## Notable Additions

This Dockerfile could be used to use CKAN workers by setting
`CKAN_WORKER="true"` environment variable.
